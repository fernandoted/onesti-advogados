{# {% set logo = theme_var(mobile ? 'custom_logo_mobile' : 'custom_logo') %}
<a href="{{ home_url }}" class="navbar-brand mr-10">
{% if logo %}
  {% set logo_file = (logo|first).name %}
  <img src="{{ url('theme://images/logo/' ~ logo_file)  }}" alt="{{ site.name }}" />
{% else %}
  {% include('@images/logo.png') %}
{% endif %}
</a> #}

<img class="main-logo" src="{{ url('theme://images/logo/logo-white-02.png') }}" alt="{{ site.name }}" />